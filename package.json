{
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Workspace root for fnaudiofx",
  "scripts": {
    "build": "pnpm --filter fnaudiofx build",
    "test": "pnpm --filter fnaudiofx-tests test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
